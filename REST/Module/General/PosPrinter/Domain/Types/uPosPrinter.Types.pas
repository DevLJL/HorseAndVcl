unit uPosPrinter.Types;

interface

{$SCOPEDENUMS ON}
type
  TPosPrinterModel = (Texto, EscPosEpson, EscBematech, EscDaruma, EscVox, EscDiebold, EscEpsonP2, CustomPos, EscPosStar, EscZJiang, EscGPrinter, EscDatecs, EscSunmi, Externo, Canvas, None);
  TPosPrinterPageCode = (pNone, p437, p850, p852, p860, pUTF8, p1252);

implementation

end.

